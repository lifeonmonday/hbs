{
  "pluginAlias": "SpotifyHBS",
  "pluginNodePackage": "homebridge-hbs",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Accessory Name",
        "type": "string",
        "default": "Spotify",
        "required": true
      },
      "accessoryType": {
        "title": "Accessory Type",
        "type": "string",
        "enum": ["SmartSpeaker", "AudioReceiver"],
        "default": "SmartSpeaker",
        "description": "SmartSpeaker: basic play/pause and volume. AudioReceiver: TV-style with track info display.",
        "required": true
      },
      "clientId": {
        "title": "Client ID",
        "type": "string",
        "description": "Your Spotify Developer Application Client ID.",
        "required": true
      },
      "clientSecret": {
        "title": "Client Secret",
        "type": "string",
        "description": "Your Spotify Developer Application Client Secret.",
        "required": true
      },
      "authCode": {
        "title": "Authorization Code",
        "type": "string",
        "description": "Paste the 'code' parameter from the Spotify callback URL for initial token setup."
      },
      "refreshToken": {
        "title": "Refresh Token",
        "type": "string",
        "description": "Optional: Existing Refresh Token if already generated."
      },
      "deviceId": {
        "title": "Spotify Device ID",
        "type": "string",
        "description": "Target Spotify Connect Device ID.",
        "required": true
      },
      "pollInterval": {
        "title": "Poll Interval (seconds)",
        "type": "number",
        "default": 5,
        "minimum": 1,
        "maximum": 60,
        "description": "How often to check Spotify playback state (in seconds)."
      },
      "homebridgeUrl": {
        "title": "Homebridge URL (for Wake-up Trigger)",
        "type": "string",
        "default": "http://127.0.0.1:8581",
        "description": "URL to your Homebridge instance (optional, for wake-up trigger)."
      },
      "homebridgeUsername": {
        "title": "Homebridge Username (for Wake-up Trigger)",
        "type": "string",
        "description": "Optional: Username for Homebridge auth (needed for wake-up trigger)."
      },
      "homebridgePassword": {
        "title": "Homebridge Password (for Wake-up Trigger)",
        "type": "string",
        "description": "Optional: Password for Homebridge auth (needed for wake-up trigger)."
      },
      "triggerSwitchUuid": {
        "title": "Trigger Switch UUID (for Wake-up)",
        "type": "string",
        "description": "Optional: UUID of a switch accessory to trigger for wake-up."
      }
    }
  }
}
