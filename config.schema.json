{
  "pluginAlias": "SpotifyHBS",
  "pluginNodePackage": "homebridge-hbs",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "Spotify",
        "required": true
      },
      "clientId": {
        "title": "Spotify Client ID",
        "type": "string",
        "description": "Your Spotify Developer Application Client ID.",
        "required": true
      },
      "clientSecret": {
        "title": "Spotify Client Secret",
        "type": "string",
        "description": "Your Spotify Developer Application Client Secret.",
        "required": true
      },
      "refreshToken": {
        "title": "Spotify Refresh Token",
        "type": "string",
        "description": "Your Spotify Refresh Token."
      },
      "authCode": {
        "title": "Spotify Authorization Code",
        "type": "string",
        "description": "Paste the 'code' parameter from the callback URL for initial token setup."
      },
      "pollInterval": {
        "title": "Default Poll Interval (seconds)",
        "type": "number",
        "default": 5,
        "minimum": 1,
        "maximum": 60,
        "description": "How often to check Spotify playback state (in seconds)."
      },
      "accessories": {
        "title": "Accessories",
        "type": "array",
        "description": "Configure the Spotify accessories you want to expose to HomeKit.",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "title": "Accessory Name",
              "type": "string",
              "placeholder": "Living Room Speaker",
              "required": true
            },
            "accessoryType": {
              "title": "Accessory Type",
              "type": "string",
              "enum": ["SmartSpeaker", "AudioReceiver"],
              "default": "SmartSpeaker",
              "description": "SmartSpeaker: media tile with Play/Pause & Volume. AudioReceiver: TV-style AVR with Now Playing track info & volume slider.",
              "required": true
            },
            "deviceId": {
              "title": "Spotify Device ID",
              "type": "string",
              "description": "Target Spotify Connect Device ID.",
              "required": true
            },
            "triggerSwitchUuid": {
              "title": "Custom Wake-up Switch UUID",
              "type": "string",
              "description": "Optional: Specific switch UUID to trigger for this device (overrides global trigger)."
            },
            "pollInterval": {
              "title": "Custom Poll Interval (seconds)",
              "type": "number",
              "minimum": 1,
              "maximum": 60,
              "description": "Optional: Custom polling interval for this accessory."
            }
          }
        }
      },
      "homebridgeUrl": {
        "title": "Homebridge URL (for Wake-up Trigger)",
        "type": "string",
        "default": "http://127.0.0.1:8581",
        "description": "URL to your Homebridge instance (optional, for wake-up trigger)."
      },
      "homebridgeUsername": {
        "title": "Homebridge Username (for Wake-up Trigger)",
        "type": "string",
        "description": "Optional: Username for Homebridge auth (needed for wake-up trigger)."
      },
      "homebridgePassword": {
        "title": "Homebridge Password (for Wake-up Trigger)",
        "type": "string",
        "description": "Optional: Password for Homebridge auth (needed for wake-up trigger)."
      },
      "triggerSwitchUuid": {
        "title": "Global Wake-up Switch UUID",
        "type": "string",
        "description": "Optional: Default UUID of a switch accessory to trigger for wake-up."
      }
    }
  },
  "layout": [
    "name",
    {
      "type": "fieldset",
      "title": "Spotify Credentials",
      "items": [
        "clientId",
        "clientSecret",
        "refreshToken",
        "authCode"
      ]
    },
    {
      "type": "fieldset",
      "title": "Spotify Accessories",
      "items": [
        {
          "key": "accessories",
          "type": "array",
          "items": [
            "accessories[].name",
            "accessories[].accessoryType",
            "accessories[].deviceId",
            "accessories[].triggerSwitchUuid",
            "accessories[].pollInterval"
          ]
        }
      ]
    },
    {
      "type": "fieldset",
      "title": "Optional: Wake-up Trigger & Advanced",
      "expandable": true,
      "items": [
        "pollInterval",
        "triggerSwitchUuid",
        "homebridgeUrl",
        "homebridgeUsername",
        "homebridgePassword"
      ]
    }
  ]
}
